---
type: index
---

# Wiki Index — Router

_Last updated: — · 0 sources, 0 pages, 0 outputs_

This is a thin router. Each domain gets its own sub-index with the full catalog under `wiki/index/`. On ingest, update the sub-index for the page's domain; only touch this router when a whole domain is added or removed.

## Domains
| Sub-index | Scope |
|-----------|-------|
| _(none yet — add `[[index/<domain>]]` when you create your first domain)_ | |

A domain is the top-level partition of your knowledge (e.g. `work`, `health`, `projects`, `reference`). See `wiki/_schema.md` for the sub-index format.
