# raw/ — immutable sources

Drop source documents here (articles, transcripts, exports, notes you want to ingest). The agent **reads** from `raw/` and **never modifies** it. Ingested knowledge is written to `wiki/`, not back here.

Keep originals verbatim. If you need a cleaned version, write it as a wiki source page, not by editing the raw file.
