# outputs/ — generated artifacts

Generated artifacts live here: comparison tables, slide decks (Marp), charts, exported analyses. Link each one from the relevant wiki page so it is reachable, and add a row to the domain sub-index `Outputs` table.

Convention: one folder per project/campaign, or `<slug>-YYYY-MM-DD.md` for one-off analyses.
